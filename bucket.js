// On clicking remove button the item should be removed from DOM as well as localstorage.
let addeddata=JSON.parse(localStorage.getItem("coffee"));
let sum=0;
for(let i=0;i<addeddata.length;i++)
{
    sum+=
}